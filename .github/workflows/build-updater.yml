name: Build Renegade X Updater Windows Image

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Updater container image tag to publish"
        required: true
        default: "1.2.1109-core20-ltsc2022-updater-r2"
      rolling_tag:
        description: "Optional rolling image tag to also publish"
        required: false
        default: "stable-updater-core20-ltsc2022"
      base_image:
        description: "Known-installable full image to layer the updater onto"
        required: true
        default: "ghcr.io/twistedbobross/renegade-x-gsa-windows:1.0.1022-core20-ltsc2022-r12"
permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: windows-2022
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure Windows Docker daemon
        shell: powershell
        run: |
          $service = Get-Service -Name docker -ErrorAction Stop
          if ($service.Status -ne "Running") {
            Start-Service -Name docker
          }

          $deadline = (Get-Date).AddMinutes(3)
          do {
            docker info *> $null
            if ($LASTEXITCODE -eq 0) {
              Write-Host "Docker daemon is ready."
              break
            }
            Start-Sleep -Seconds 5
          } while ((Get-Date) -lt $deadline)

          if ($LASTEXITCODE -ne 0) {
            throw "Docker daemon did not become ready within three minutes."
          }

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build updater image
        shell: powershell
        run: |
          $repo = "${{ github.repository }}".ToLowerInvariant()
          $image = "ghcr.io/${repo}:${{ inputs.image_tag }}"
          docker build `
            --build-arg BASE_IMAGE="${{ inputs.base_image }}" `
            -f Dockerfile.updater `
            -t $image `
            .

      - name: Verify updater image defaults
        shell: powershell
        run: |
          $repo = "${{ github.repository }}".ToLowerInvariant()
          $image = "ghcr.io/${repo}:${{ inputs.image_tag }}"
          $imageEnv = docker image inspect $image --format '{{json .Config.Env}}' | ConvertFrom-Json
          foreach ($expected in @(
            "RENX_RUNTIME_AUTO_UPDATE=true",
            "RENX_DEFER_FIRST_RUNTIME_UPDATE=true",
            "RENX_UPDATE_MANIFEST_URL=https://raw.githubusercontent.com/TwistedBobRoss/Renegade-X-GSA-Windows/main/release-manifest.json",
            "RENX_UPDATE_CHANNEL=stable"
          )) {
            if ($imageEnv -notcontains $expected) {
              throw "Updater image is missing default environment value: $expected"
            }
          }

      - name: Smoke test updater image startup
        timeout-minutes: 8
        shell: powershell
        run: |
          $repo = "${{ github.repository }}".ToLowerInvariant()
          $image = "ghcr.io/${repo}:${{ inputs.image_tag }}"
          $name = "renx-updater-smoke"
          $hostData = Join-Path $env:RUNNER_TEMP "renx-updater-smoke-data"
          $hostConfig = Join-Path $hostData "Config"

          $existing = docker ps -aq --filter "name=^/${name}$"
          if ($existing) {
            docker rm -f $name | Out-Null
          }
          Remove-Item -LiteralPath $hostData -Recurse -Force -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Force -Path $hostConfig | Out-Null

          @"
[UTGame.UTGame]
GameSpecificMapCycles=(GameClassName="Rx_Game",Maps=("CNC-Field","CNC-Walls","CNC-Mesa"))
"@ | Set-Content -LiteralPath (Join-Path $hostConfig "UDKGame.ini") -Encoding UTF8

          @"
[RenX_Game.Rx_Game]
MaxMapVoteSize=7
RecentMapsToExclude=1
MapVoteTime=42
ChangeMapDisabledTime=123
SurrenderLength=321
SurrenderDisabledTime=654
bAdminsStartMapVote=true
bBotVotesDisabled=true
bRemoveVariantMapsInVoteList=false
"@ | Set-Content -LiteralPath (Join-Path $hostConfig "UDKRenegadeX.ini") -Encoding UTF8

          docker run -d --name $name `
            -v "${hostData}:C:\renx-data" `
            -e RENX_SERVER_NAME="Updater Smoke Test" `
            -e RENX_MAP="CNC-Field" `
            -e RENX_MAX_PLAYERS="2" `
            -e RENX_GAME_PORT="7777" `
            -e RENX_PEER_PORT="7778" `
            -e RENX_QUERY_PORT="27015" `
            -e RENX_RCON_PORT="37015" `
            -e RENX_WEB_PORT="6969" `
            -e RENX_LISTED="false" `
            -e RENX_MARATHON_MODE="true" `
            -e RENX_RUNTIME_AUTO_UPDATE="false" `
            -e RENX_UPDATE_CHANNEL="stable" `
            $image | Out-Null

          try {
            $logPath = "C:\renx-data\Logs\RenegadeXServer.log"
            $deadline = (Get-Date).AddMinutes(4)
            $logReady = $false
            do {
              Start-Sleep -Seconds 10
              $state = docker inspect $name | ConvertFrom-Json
              if (-not $state[0].State.Running) {
                docker logs --tail 250 $name
                throw "Renegade X updater smoke-test container exited with code $($state[0].State.ExitCode)."
              }

              docker exec $name powershell -NoProfile -Command "if (-not (Test-Path -LiteralPath '$logPath')) { exit 1 }; `$content = Get-Content -Raw -LiteralPath '$logPath'; if (`$content -match 'Game engine initialized|MAP Loaded') { exit 0 }; exit 1"
              $logReady = $LASTEXITCODE -eq 0
            } while (-not $logReady -and (Get-Date) -lt $deadline)

            if (-not $logReady) {
              docker logs --tail 250 $name
              docker exec $name powershell -NoProfile -Command "if (Test-Path -LiteralPath '$logPath') { Get-Content -LiteralPath '$logPath' -Tail 100 }"
              throw "Renegade X did not reach map/game-engine initialization within four minutes."
            }

            $gameLog = docker exec $name powershell -NoProfile -Command "Get-Content -Raw -LiteralPath '$logPath'" | Out-String
            Write-Host $gameLog
            if (
              $gameLog -notmatch "bIsLanMatch=false" -or
              $gameLog -notmatch "-port=7777" -or
              $gameLog -notmatch "-QueryPort=27015"
            ) {
              throw "Renegade X game log did not confirm internet mode and assigned ports."
            }

            $checks = [ordered]@{
              MaxMapVoteSize = "7"
              RecentMapsToExclude = "1"
              MapVoteTime = "42"
              ChangeMapDisabledTime = "123"
              SurrenderLength = "321"
              SurrenderDisabledTime = "654"
              bAdminsStartMapVote = "true"
              bBotVotesDisabled = "true"
              bRemoveVariantMapsInVoteList = "false"
            }
            foreach ($target in @("C:\renx-data\ServerFiles\UDKGame\Config\UDKRenegadeX.ini", "C:\renx-data\ServerFiles\UDKGame\Config\DefaultRenegadeX.ini")) {
              foreach ($key in $checks.Keys) {
                $actual = docker exec $name powershell -NoProfile -Command "(Select-String -LiteralPath '$target' -Pattern '^$key=(.*)$').Matches.Groups[1].Value" | Out-String
                $actual = $actual.Trim()
                if ($actual -ne $checks[$key]) {
                  throw "INI vote check failed for $key in $target. Expected '$($checks[$key])', got '$actual'."
                }
              }
            }

            foreach ($target in @("C:\renx-data\ServerFiles\UDKGame\Config\UDKGame.ini", "C:\renx-data\ServerFiles\UDKGame\Config\DefaultGame.ini")) {
              $rotation = docker exec $name powershell -NoProfile -Command "(Select-String -LiteralPath '$target' -Pattern '^GameSpecificMapCycles=').Line" | Out-String
              if ($rotation -notmatch "CNC-Mesa") {
                throw "Map rotation check failed for $target. CNC-Mesa was not preserved."
              }
            }

            $mapList = docker exec $name powershell -NoProfile -Command "Get-Content -Raw -LiteralPath 'C:\renx-data\ServerFiles\UDKGame\Config\UDKMapList.ini'" | Out-String
            if ($mapList -notmatch 'Map="CNC-Mesa"') {
              throw "UDKMapList.ini did not include the INI-sourced CNC-Mesa rotation."
            }
          }
          finally {
            docker rm -f $name | Out-Null
          }

      - name: Push updater image
        shell: powershell
        run: |
          $repo = "${{ github.repository }}".ToLowerInvariant()
          $image = "ghcr.io/${repo}:${{ inputs.image_tag }}"
          docker push $image

          $rollingTag = "${{ inputs.rolling_tag }}"
          if (-not [string]::IsNullOrWhiteSpace($rollingTag)) {
            $rollingImage = "ghcr.io/${repo}:$rollingTag"
            docker tag $image $rollingImage
            docker push $rollingImage
          }
